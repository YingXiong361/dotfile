# System shortcuts
alias ll='ls -alhF --color=auto'
alias grep='grep --color=auto'
alias tmux="tmux -2"            " Force 256-color mode

# Git shortcuts
alias gs="git status"
alias gc="git commit -m"

# Tmux session management
alias tls="tmux ls"
alias ta="tmux attach -t" [2,5](@ref)
